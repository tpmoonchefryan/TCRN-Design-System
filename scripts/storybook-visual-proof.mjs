// RETIRED — TCRN-DS-STORY-041 (INIT-007 fidelity closure), 2026-07-23.
//
// The legacy exact-PNG visual oracle `canonicalized_raw_png_exact_v1` (baseline under
// docs/verification/storybook-visual-proof/, sourceHead ec57b606, 134 screenshots) is
// retired. Its committed PNG baseline had drifted many commits and mismatched 134/134, and
// per the INIT-002 ruling ("don't sign in unreproducible values") an unreproducible
// exact-PNG signature must not stand as an oracle.
//
// The live doc-shell visual baseline is now the internal-alpha PERCEPTUAL visual-signature
// system:
//   - baseline: docs/verification/internal-alpha/visual-signature-baseline.json
//   - tolerance: meanAbsolute<=2 / maxCell<=8
//   - enforced by: scripts/internal-alpha-browser-proof.mjs (run via `pnpm internal-alpha:proof`)
//
// This script is NOT part of `pnpm verify`. It remains only as a discoverable pointer so
// `pnpm storybook:visual-proof` explains the retirement instead of failing on stale bytes.
// The docs/verification/storybook-visual-proof/ directory is kept as pre-INIT-001 history
// (its README carries the retirement banner) and is still read by the privacy / no-overclaim
// scans; it is no longer generated by this script.

const lines = [
  "storybook:visual-proof is RETIRED (TCRN-DS-STORY-041).",
  "The exact-PNG oracle canonicalized_raw_png_exact_v1 is superseded by the internal-alpha",
  "perceptual visual-signature baseline (tolerance meanAbsolute<=2 / maxCell<=8).",
  "Run `pnpm internal-alpha:proof` instead; the baseline lives at",
  "docs/verification/internal-alpha/visual-signature-baseline.json.",
  "docs/verification/storybook-visual-proof/ is retained as pre-INIT-001 history only."
];

for (const line of lines) {
  console.log(line);
}

process.exit(0);
