@AGENTS.md

## Localization

The canonical policy requires every visible string to cover `zh-CN`, `en`, `ja`, `ko`,
and `fr`. Use `scripts/lib/locale-invariant-ledger.mjs` only for deliberate machine-token
exceptions, and verify the binding with `internal-alpha:proof`.
